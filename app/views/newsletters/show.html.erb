<div class="index-container">
  <div class="section-title-container">
    <h4 class="section-title"><%=@newsletter.date%> Newsletter: </h4>
  </div>
  <% @stories.each do |story| %>
      <%= render partial: "story", locals: {story: story} %>    
  <% end %>
</div>